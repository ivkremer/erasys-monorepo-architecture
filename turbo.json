{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["packages/**/dist/**", "apps/web-next/.next/**", "apps/web-spa/dist/**"]
    },
    "lint": {},
    "test": {},
    "prettier": {}
  }
}
